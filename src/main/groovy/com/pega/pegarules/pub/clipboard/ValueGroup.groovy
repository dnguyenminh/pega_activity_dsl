package com.pega.pegarules.pub.clipboard

/** Thin wrapper representing a grouped set of values (map). */
class ValueGroup extends SimpleClipboardProperty {
    ValueGroup(Map m = null) {
        super(m)
    }
}
